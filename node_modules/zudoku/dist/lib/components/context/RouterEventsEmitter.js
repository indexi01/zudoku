import { useEffect, useRef } from "react";
import { useLocation } from "react-router";
import { useZudoku } from "./ZudokuContext.js";
export const RouterEventsEmitter = () => {
    const location = useLocation();
    const zudoku = useZudoku();
    const previousLocation = useRef(undefined);
    useEffect(() => {
        zudoku.emitEvent("location", {
            from: previousLocation.current,
            to: location,
        });
        previousLocation.current = location;
    }, [zudoku, location]);
    return null;
};
//# sourceMappingURL=RouterEventsEmitter.js.map