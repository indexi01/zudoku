import { type ReactNode } from "react";
type PathParamProps = {
    name: string;
    index: number;
    originalValue?: string;
};
export declare const PathRenderer: ({ path, renderParam, }: {
    path: string;
    renderParam: (props: PathParamProps) => ReactNode;
}) => import("react/jsx-runtime").JSX.Element[];
export {};
