import type { JSONSchema } from "./index.js";
/**
 * Resolves a $ref pointer in a schema and returns the referenced value.
 */
export declare const resolveLocalRef: (schema: JSONSchema, ref: string) => unknown;
