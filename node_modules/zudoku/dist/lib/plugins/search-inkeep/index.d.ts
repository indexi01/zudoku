import type { ZudokuPlugin } from "../../core/plugins.js";
interface PluginInkeepBaseSettings {
    apiKey?: string;
    integrationId: string;
    organizationId: string;
    organizationDisplayName?: string;
    primaryBrandColor: string;
}
interface InkeepEmbedConfig {
    componentType: string;
    targetElement: HTMLElement;
    properties: unknown;
}
interface InkeepWidget {
    render: (config: InkeepEmbedConfig & {
        isOpen: boolean;
    }) => void;
}
declare global {
    let Inkeep: () => {
        embed: (config: InkeepEmbedConfig) => InkeepWidget;
    };
}
export declare const inkeepSearchPlugin: (settings: PluginInkeepBaseSettings) => ZudokuPlugin;
export {};
