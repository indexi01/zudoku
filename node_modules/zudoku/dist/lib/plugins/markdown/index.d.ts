import type { Toc } from "@stefanprobst/rehype-extract-toc";
import type { MDXProps } from "mdx/types.js";
import { type JSX } from "react";
import { ZudokuDocsConfig } from "../../../config/validators/common.js";
import type { ZudokuPlugin } from "../../core/plugins.js";
export interface MarkdownPluginOptions extends ZudokuDocsConfig {
    fileImports: Record<string, () => Promise<MDXImport>>;
}
export type MarkdownPluginDefaultOptions = Pick<Frontmatter, "toc" | "disablePager">;
export type Frontmatter = {
    title?: string;
    description?: string;
    category?: string;
    toc?: boolean;
    disablePager?: boolean;
};
export type MDXImport = {
    tableOfContents: Toc;
    frontmatter: Frontmatter;
    excerpt?: string;
    default: (props: MDXProps) => JSX.Element;
};
export declare const markdownPlugin: (options: MarkdownPluginOptions[]) => ZudokuPlugin;
