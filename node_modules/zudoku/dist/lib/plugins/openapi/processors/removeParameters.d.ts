import type { ProcessorArg } from "../../../../config/validators/BuildSchema.js";
import type { OpenAPIDocument } from "../../../oas/parser/index.js";
import { type RecordAny } from "./traverse.js";
interface RemoveParametersOptions {
    names?: string[];
    in?: string[];
    shouldRemove?: ({ parameter }: {
        parameter: RecordAny;
    }) => boolean;
}
export declare const removeParameters: ({ names, in: locations, shouldRemove }?: RemoveParametersOptions) => ({ schema }: ProcessorArg) => OpenAPIDocument;
export {};
