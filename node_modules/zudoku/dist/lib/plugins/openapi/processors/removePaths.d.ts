import type { ProcessorArg } from "../../../../config/validators/BuildSchema.js";
import type { OpenAPIDocument } from "../../../oas/parser/index.js";
import { type RecordAny } from "./traverse.js";
interface RemovePathsOptions {
    paths?: Record<string, true | string[]>;
    shouldRemove?: (options: {
        path: string;
        method: true | string;
        operation: RecordAny;
    }) => boolean;
}
export declare const removePaths: ({ paths, shouldRemove }: RemovePathsOptions) => ({ schema }: ProcessorArg) => OpenAPIDocument;
export {};
