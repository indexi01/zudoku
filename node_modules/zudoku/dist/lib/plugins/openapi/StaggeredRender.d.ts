import { ReactNode } from "react";
export declare const StaggeredRenderContext: import("react").Context<{
    stagger: boolean;
}>;
declare const StaggeredRender: ({ children }: {
    children: ReactNode[];
}) => ReactNode[];
export default StaggeredRender;
