import { create } from "zustand";
import { createJSONStorage, persist } from "zustand/middleware";
export const useIdentityStore = create()(persist((set, get) => ({
    rememberedIdentity: null,
    setRememberedIdentity: (identity) => set({ rememberedIdentity: identity }),
    getRememberedIdentity: (availableIdentities) => availableIdentities.find((identity) => identity === get().rememberedIdentity),
}), {
    name: "identity-storage",
    storage: createJSONStorage(() => sessionStorage),
}));
//# sourceMappingURL=rememberedIdentity.js.map