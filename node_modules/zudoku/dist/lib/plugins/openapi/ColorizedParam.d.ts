import { type ReactNode } from "react";
export declare const DATA_ATTR = "data-linked-param";
export declare const usePastellizedColor: (name: string) => {
    text: string;
    background: string;
};
export declare const useParamColor: (name: string) => {
    text: string;
    background: string;
};
export declare const ColorizedParam: ({ name, className, slug, title, children, onClick, }: {
    name: string;
    className?: string;
    backgroundOpacity?: string;
    borderOpacity?: string;
    slug?: string;
    children?: ReactNode;
    title?: string;
    onClick?: () => void;
}) => import("react/jsx-runtime").JSX.Element;
