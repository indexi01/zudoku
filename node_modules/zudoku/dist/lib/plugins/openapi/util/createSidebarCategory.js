import { MethodColorMap } from "./methodColorMap.js";
export const createSidebarCategory = ({ label, path, operations, collapsible, collapsed, }) => ({
    type: "category",
    label,
    link: {
        type: "doc",
        id: path,
        label,
    },
    collapsible,
    collapsed,
    items: operations.map((operation) => ({
        type: "link",
        label: operation.summary ?? operation.path,
        href: `${path}#${operation.slug}`,
        badge: {
            label: operation.method,
            color: MethodColorMap[operation.method.toLowerCase()],
            invert: true,
        },
    })),
});
//# sourceMappingURL=createSidebarCategory.js.map