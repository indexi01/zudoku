interface SelectedServerState {
    selectedServer?: string;
    setSelectedServer: (newServer: string) => void;
}
export declare const useSelectedServerStore: import("zustand").UseBoundStore<Omit<import("zustand").StoreApi<SelectedServerState>, "persist"> & {
    persist: {
        setOptions: (options: Partial<import("zustand/middleware").PersistOptions<SelectedServerState, SelectedServerState>>) => void;
        clearStorage: () => void;
        rehydrate: () => Promise<void> | void;
        hasHydrated: () => boolean;
        onHydrate: (fn: (state: SelectedServerState) => void) => () => void;
        onFinishHydration: (fn: (state: SelectedServerState) => void) => () => void;
        getOptions: () => Partial<import("zustand/middleware").PersistOptions<SelectedServerState, SelectedServerState>>;
    };
}>;
/**
 * Simple wrapper for `useSelectedServerStore` to fall back to first of the provided servers
 */
export declare const useSelectedServer: (servers: Array<{
    url: string;
}>) => {
    selectedServer: string;
    setSelectedServer: (newServer: string) => void;
};
export {};
