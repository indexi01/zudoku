import { type PluginOption } from "vite";
import { type LoadedConfig } from "../config/config.js";
export default function vitePlugin(initialConfig: LoadedConfig, onConfigChange?: () => Promise<LoadedConfig>): PluginOption;
