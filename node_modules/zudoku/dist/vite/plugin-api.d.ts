import { type Plugin } from "vite";
import { type LoadedConfig } from "../config/config.js";
declare const viteApiPlugin: (getConfig: () => LoadedConfig) => Promise<Plugin>;
export default viteApiPlugin;
