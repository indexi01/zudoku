import type { PrerenderResponse } from "./PrerenderResponse.js";
export declare class FileWritingResponse implements PrerenderResponse {
    private resolve;
    private resolved;
    private dontSave;
    buffer: string;
    redirectedTo?: string;
    statusCode: number;
    options: {
        fileName: string;
        writeRedirects: boolean;
    };
    set(): void;
    status(status: number): void;
    on(): void;
    constructor(options: {
        fileName: string;
        writeRedirects: boolean;
    });
    redirect(status: number, url: string): void;
    send(chunk: string): Promise<void>;
    write(chunk: string, _encoding?: string): void;
    end(chunk?: string): Promise<void>;
    isSent(): Promise<void>;
}
