export type WorkerResult = {
    outputPath: string;
    html: string;
    redirect?: {
        from: string;
        to: string;
    };
};
export declare const prerender: ({ html, dir, basePath, serverConfigFilename, writeRedirects, }: {
    html: string;
    dir: string;
    basePath?: string;
    serverConfigFilename: string;
    writeRedirects: boolean;
}) => Promise<WorkerResult[]>;
