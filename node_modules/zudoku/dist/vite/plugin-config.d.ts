import { type Plugin } from "vite";
import type { LoadedConfig } from "../config/config.js";
declare const viteConfigPlugin: (getConfig: () => LoadedConfig) => Plugin;
export default viteConfigPlugin;
