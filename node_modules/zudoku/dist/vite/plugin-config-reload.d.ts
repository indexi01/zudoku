import { type Plugin, type ViteDevServer } from "vite";
import { type LoadedConfig } from "../config/config.js";
export declare const reload: ({ ws, environments }: ViteDevServer) => void;
export declare const createConfigReloadPlugin: (initialConfig: LoadedConfig, onConfigChange?: () => Promise<LoadedConfig>) => [Plugin, () => LoadedConfig];
