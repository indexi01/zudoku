import { type Plugin } from "vite";
import { type LoadedConfig } from "../config/config.js";
declare const viteRedirectPlugin: (getConfig: () => LoadedConfig) => Plugin;
export default viteRedirectPlugin;
