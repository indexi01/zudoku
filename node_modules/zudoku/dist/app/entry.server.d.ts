import type express from "express";
import { type RouteObject } from "react-router";
import "virtual:zudoku-theme.css";
import "vite/modulepreload-polyfill";
import type { PrerenderResponse } from "../vite/prerender/PrerenderResponse.js";
import "./main.css";
import { getRoutesByConfig } from "./main.js";
export { getRoutesByConfig };
export declare const render: ({ template, request: baseRequest, response, routes, basePath, bypassProtection, }: {
    template: string;
    request: express.Request | Request;
    response: express.Response | PrerenderResponse;
    routes: RouteObject[];
    basePath?: string;
    bypassProtection?: boolean;
}) => Promise<void>;
export declare function createFetchRequest(req: express.Request, res: express.Response | PrerenderResponse): Request;
