import { type Argv } from "yargs";
export type Arguments = {
    dir: string;
    port?: number;
};
declare const previewCommand: {
    desc: string;
    command: string;
    builder: (yargs: Argv) => Argv<{
        dir: string;
    } & {
        port: number | undefined;
    }>;
    handler: (argv: Arguments) => Promise<void>;
};
export default previewCommand;
